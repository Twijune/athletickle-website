# Release and capture checklist

## Website review

- Verify `/`, `/parkour`, `/strength`, `/about` at mobile and desktop widths.
- Switch homepage through EN/FR/DE/ES. New product and founder pages are explicitly English.
- Follow every primary CTA to the valid public Discord invitation; verify it as a new member.
- Confirm each product page has its own canonical URL, description, social card and sitemap entry.
- Confirm all pages remain readable before JavaScript hydration and that no stale screenshots or unsupported claims are rendered.
- Review the existing contact/legal changes already in the repository separately. This marketing implementation preserves them.
- Native-speaker editorial review is recommended before localized promotion; machine-authored translations are included for consistency.

## Real screenshots still needed

No Android device was connected during implementation. Old broken-icon screenshots were removed from the visible site. Labelled HTML program illustrations are the interim replacement.

Capture from the actual candidate build on Android/iOS as available:
1. Parkour creation inputs with an ordinary alias; no “Hello world” or private data.
2. Parkour roadmap and first-session detail with all icons rendering.
3. Strength creation with two-day mode and a genuine generated first session.
4. Strength load-direction cue using labelled test logs.
5. An eligible exercise swap and an external demo opening successfully.

Check selected profile, app version and screenshots together. Do not manually replace exercise doses in a screenshot. Crop only irrelevant device chrome. Store resulting images in `public/screenshots/` with edition/version in filenames and a short capture ledger. Replace ProgramPreview only after these are reviewed; preserve descriptive alt text and explicit edition identity.

## Beta readiness

- Install the actual build from its actual distribution URL on each offered platform.
- Create → inspect → eligible swap → save → reopen → first session → log/export.
- Verify free first block, subscription flow and restore behavior with the app release checklist, not marketing code.
- Test offline behavior and verify demonstrated video URLs.
- Record and communicate known limitations; do not promise an unavailable platform or release date.

## People and proof

- Public name/title confirmed: Mika — Founder of Athletickle. Approved interim photo installed at `public/founder-mika.jpg`; studio replacement pending.
- Never name athletes, teams or clients. Do not publish identifying details or implied endorsements.
- Ask five unfamiliar visitors which app is for them, what is free and how to join.
- Gather completed-block feedback four weeks after each tester's start, not at an arbitrary campaign deadline.

## Publication and outreach

The local implementation does not deploy the website, change Discord or send messages. Review the built site, then deploy through the project's normal workflow. Create the server structure and creator invites in the actual Discord server. Replace availability CTAs only when real store URLs are confirmed.
